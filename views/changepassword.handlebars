   <div>
       
            <div class="container">
               <div class="row">
              
                <form action="/changepassword" method="POST" class="form-horizontal loginwrap">
                {{#if flash}}
<div class="alert alert-dismissible alert-{{flash.type}}">
<button type="button" class="close"
data-dismiss="alert" aria-hidden="true">&times;</button>
<strong>{{flash.intro}}</strong> {{{flash.message}}}
</div>
{{/if}}
                   <h2 class="text-center text-primary"><span class="glyphicon glyphicon-lock"></span>&nbsp;<b>CHANGE PASSWORD</b></h2>
                   <hr />
                     <div class="form-group">
                        <label class="control-label sr-only" for="currentpwd">Current Password:</label>
                        <div class="col-xs-12">
                            <input type="password" name="currentpassword" class="form-control" id="currentpwd" placeholder="Enter current password">
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="control-label sr-only" for="newpwd">New Password:</label>
                        <div class="col-xs-12">
                            <input type="password" name="newpassword" class="form-control" id="newpwd" placeholder="Enter new password">
                        </div>
                    </div>
                     <div class="form-group">
                        <label class="control-label sr-only" for="confirmnewpwd">Confirm New Password:</label>
                        <div class="col-xs-12">
                            <input type="password" name="confirmnewpassword" class="form-control" id="confirmnewpwd" placeholder="Confirm new password">
                        </div>
                    </div>
                    <div class="form-group">
                        
                         <div class="col-xs-offset-0 col-xs-12 text-right">
                              <button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-lock"></span>&nbsp; Change Password</button>
                             
                        </div>
                    </div>
                   
                </form>
              </div>
                
                </div>

            </div>